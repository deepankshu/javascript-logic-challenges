// Array Iteration Demo
// Demonstrates looping through an array using a for loop.

const fruits = ['Apple', 'Banana', 'Mango', 'Orange'];

for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}